---
description: 
globs: 
alwaysApply: false
---
# CheckMate – Post‑Task Verification
- After coding, verify every spec reported in scope analysis
  - Execute: node scripts/cm-enforce.js run \
             --target "$(jq -r '.[]' .cursor/cm_list.json)" \
             --fail-early
- Cursor keeps iterating until the command prints **[CM‑PASS]**
